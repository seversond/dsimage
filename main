apt-get update
apt-get install ufw
ufw default deny incoming
ufw default allow outgoing
ufw limit ssh
ufw --force enable
#wget
#apt-get install $(grep -vE "^\s*#" packages | tr "\n" " ")
#dpkg -i <rstudio.deb>
